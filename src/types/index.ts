export interface SearchFilter {
  categories: string[];
  database: boolean;
  yolo: string;
  conf: number;
  movieDatabase: string;
  genres: string[];
  query: string;
  from: string;
  to: string;
}
